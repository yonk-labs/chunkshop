"""Infrastructure layer lifted from RAGFlow's common/data_source/ tree.

Each file in this package preserves the upstream copyright + licence
header (MIT Expat, Onyx-attributed) verbatim. Cross-file imports have
been rewritten from `common.data_source.*` to `chunkshop_connectors._base.*`
per the lift table; the audit trail lives in
`chunkshop_connectors/_PROVENANCE.md`.
"""
