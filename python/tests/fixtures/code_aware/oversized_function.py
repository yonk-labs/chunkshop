"""A single function with a deliberately oversized body (~5000+ chars).

Used to test the oversize-handling branch of the code_aware chunker. The body
is just deterministic placeholder statements repeated many times.
"""


def big_function():
    """A function whose body exceeds max_chars on purpose."""
    result = []
    result.append("alpha line of placeholder text used to grow the body size")
    result.append("beta line of placeholder text used to grow the body size")
    result.append("gamma line of placeholder text used to grow the body size")
    result.append("delta line of placeholder text used to grow the body size")
    result.append("epsilon line of placeholder text used to grow the body size")
    result.append("zeta line of placeholder text used to grow the body size")
    result.append("eta line of placeholder text used to grow the body size")
    result.append("theta line of placeholder text used to grow the body size")
    result.append("iota line of placeholder text used to grow the body size")
    result.append("kappa line of placeholder text used to grow the body size")
    result.append("lambda line of placeholder text used to grow the body size")
    result.append("mu line of placeholder text used to grow the body size")
    result.append("nu line of placeholder text used to grow the body size")
    result.append("xi line of placeholder text used to grow the body size")
    result.append("omicron line of placeholder text used to grow the body size")
    result.append("pi line of placeholder text used to grow the body size")
    result.append("rho line of placeholder text used to grow the body size")
    result.append("sigma line of placeholder text used to grow the body size")
    result.append("tau line of placeholder text used to grow the body size")
    result.append("upsilon line of placeholder text used to grow the body size")
    result.append("phi line of placeholder text used to grow the body size")
    result.append("chi line of placeholder text used to grow the body size")
    result.append("psi line of placeholder text used to grow the body size")
    result.append("omega line of placeholder text used to grow the body size")
    result.append("alpha line of placeholder text used to grow the body size")
    result.append("beta line of placeholder text used to grow the body size")
    result.append("gamma line of placeholder text used to grow the body size")
    result.append("delta line of placeholder text used to grow the body size")
    result.append("epsilon line of placeholder text used to grow the body size")
    result.append("zeta line of placeholder text used to grow the body size")
    result.append("eta line of placeholder text used to grow the body size")
    result.append("theta line of placeholder text used to grow the body size")
    result.append("iota line of placeholder text used to grow the body size")
    result.append("kappa line of placeholder text used to grow the body size")
    result.append("lambda line of placeholder text used to grow the body size")
    result.append("mu line of placeholder text used to grow the body size")
    result.append("nu line of placeholder text used to grow the body size")
    result.append("xi line of placeholder text used to grow the body size")
    result.append("omicron line of placeholder text used to grow the body size")
    result.append("pi line of placeholder text used to grow the body size")
    result.append("rho line of placeholder text used to grow the body size")
    result.append("sigma line of placeholder text used to grow the body size")
    result.append("tau line of placeholder text used to grow the body size")
    result.append("upsilon line of placeholder text used to grow the body size")
    result.append("phi line of placeholder text used to grow the body size")
    result.append("chi line of placeholder text used to grow the body size")
    result.append("psi line of placeholder text used to grow the body size")
    result.append("omega line of placeholder text used to grow the body size")
    result.append("alpha line of placeholder text used to grow the body size")
    result.append("beta line of placeholder text used to grow the body size")
    result.append("gamma line of placeholder text used to grow the body size")
    result.append("delta line of placeholder text used to grow the body size")
    result.append("epsilon line of placeholder text used to grow the body size")
    result.append("zeta line of placeholder text used to grow the body size")
    result.append("eta line of placeholder text used to grow the body size")
    result.append("theta line of placeholder text used to grow the body size")
    result.append("iota line of placeholder text used to grow the body size")
    result.append("kappa line of placeholder text used to grow the body size")
    result.append("lambda line of placeholder text used to grow the body size")
    result.append("mu line of placeholder text used to grow the body size")
    result.append("nu line of placeholder text used to grow the body size")
    result.append("xi line of placeholder text used to grow the body size")
    result.append("omicron line of placeholder text used to grow the body size")
    result.append("pi line of placeholder text used to grow the body size")
    result.append("rho line of placeholder text used to grow the body size")
    result.append("sigma line of placeholder text used to grow the body size")
    result.append("tau line of placeholder text used to grow the body size")
    result.append("upsilon line of placeholder text used to grow the body size")
    result.append("phi line of placeholder text used to grow the body size")
    result.append("chi line of placeholder text used to grow the body size")
    result.append("psi line of placeholder text used to grow the body size")
    result.append("omega line of placeholder text used to grow the body size")
    return result
