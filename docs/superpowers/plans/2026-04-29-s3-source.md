# s3 source — Python + Rust Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Mission Brief:** `skill-output/mission-brief/Mission-Brief-s3-source.md`.

**Goal:** Replace the NotImplementedError stubs on both sides with a real S3 source. Closes 5/5 sources on Rust.

**Architecture:** Python uses `boto3` (optional `[s3]` extra). Rust uses `object_store` crate (`aws` feature). Both honor `endpoint_url` for minio / R2 / other S3-compatible servers. Tests skip-without-env to avoid heavy mock infra.

---

## Tasks

1. **Python config:** add `endpoint_url: Optional[str] = None` to `S3Source` in `python/src/chunkshop/config.py`.
2. **Python impl:** real `iter_documents()` in `python/src/chunkshop/sources/s3.py` using boto3. Lazy import.
3. **Python pyproject:** add `s3 = ["boto3>=1.28"]` optional extra.
4. **Python test:** new `tests/chunkshop/test_s3_source.py` — skip-without-`CHUNKSHOP_S3_TEST_BUCKET`; when set, upload 3-key fixture, run source, assert 3 Documents, clean up.
5. **Rust config:** add `S3SourceConfig` variant on `SourceConfig` with `bucket`, `prefix`, `endpoint_url`.
6. **Rust deps:** add `object_store` with `aws` feature.
7. **Rust impl:** `S3Source::new` + `async fn iter_documents` in `rust/chunkshop/src/source.rs`.
8. **Rust runner:** add `Http`-style `S3` arm to `AnySource`.
9. **Rust test:** `tests/s3_source.rs` — same skip-without-env pattern.
10. **Docs:** README + CHANGELOG.
11. **DC-FINAL + finishing-a-development-branch.**
