'# NOTE': Top combo from bakeoff 'ntsb_bakeoff' (MRR=0.933, r@1=0.917). Point `source` at your real corpus before running `chunkshop ingest`.
cell_name: ntsb_bakeoff_recommended
chunker:
  max_chars: 2000
  min_section_chars: 100
  prefix_heading: true
  type: hierarchy
embedder:
  batch_size: 64
  dim: 768
  model_name: Xenova/bge-base-en-v1.5-int8
  type: fastembed
framer:
  type: identity
source:
  encoding: utf-8
  glob: /home/yonk/yonk-tools/pg-raggraph/benchmarks/kg-rag-eval/extracted/ntsb/*.md
  id_from: stem
  type: files
target:
  dsn_env: CHUNKSHOP_DSN
  mode: overwrite
  schema: chunkshop_bakeoff_ntsb
  table: ntsb_bakeoff_production
